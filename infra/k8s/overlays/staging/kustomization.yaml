apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vitasoft-staging

resources:
  - ../../base

# CD workflow sẽ set image tag ở đây:
# kustomize edit set image api=<registry>/api:<sha>
