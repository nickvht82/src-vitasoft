# Postgres connection string used by Prisma (@vitasoft/database).
# Local dev via docker-compose uses the `postgres` service below.
DATABASE_URL="postgresql://vitasoft:vitasoft@localhost:5432/vitasoft?schema=public"
